# 👤 p5 - auth + integration + payment + UI (bửu chương)
| Week | Tasks |
|------|------|
| 1 | xây chức năng đăng ký/đăng nhập: lưu thông tin user vào database, sử dụng session PHP để duy trì trạng thái đăng nhập, đồng thời xây layout chung (header, navbar, footer) để các trang dùng chung giao diện |
| 2 | tích hợp frontend với backend: gọi API từ P1 để lấy danh sách đề thi thật và hiển thị lên UI (thay cho dữ liệu dummy), xử lý trạng thái loading và lỗi khi gọi API |
| 3 | tích hợp luồng submit bài: nhận dữ liệu đáp án từ frontend (P2), gửi lên backend (P4) để chấm điểm, đảm bảo format dữ liệu đúng và xử lý phản hồi từ server |
| 4 | xây chức năng mua đề (fake payment): thêm trạng thái "đã mua" cho user/test trong database, hiển thị nút "Buy" và chỉ cho phép truy cập đề khi đã unlock |
| 5 | kiểm thử và hoàn thiện hệ thống: test toàn bộ flow (login -> chọn đề -> làm bài -> submit -> xem kết quả), fix bug liên quan integration (API, dữ liệu sai), chỉnh UI cho đồng bộ và dễ sử dụng |
